cmake_minimum_required(VERSION 3.16)

# Give your project a name and specify languages
project(smartjumprope LANGUAGES C CXX)

# Include ESP-IDF project setup
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Declare the project to ESP-IDF
project(smartjumprope)